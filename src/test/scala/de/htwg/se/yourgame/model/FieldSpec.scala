package de.htwg.se.yourgame.model

import org.scalatest._
import org.junit.runner.RunWith
import org.scalatest.junit.JUnitRunner


/**
  * Created by margogo on 15.04.17.
  */
@RunWith(classOf[JUnitRunner])
class FieldSpec extends FlatSpec with Matchers {
  "A Field" should "have all data" in {
//    val testfield = Field (1, "Property","green", isUsed = false, Array(0,1,2),Array(10,11,12))
//    testfield.id should be(1)
//    testfield.property should be("Property")
//    testfield.color should be("green")
//    testfield.isUsed should be(false)
//    testfield.predecessorIds should be(Array(0,1,2))
//    testfield.successorIds should be(Array(10,11,12))
  }
}
